#!/bin/bash

# Evaluate relationship between Italia and bigbosefx
# This script runs the relationship evaluation and outputs the result

# Change to the script directory
cd "$(dirname "$0")"

# Run the evaluation script
node run_relationship_evaluation.js
